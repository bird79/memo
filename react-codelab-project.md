## git
```
git clone https://github.com/velopert/react-codelab-project.git
cd react-codelab-project
git checkout step00
```

```
mkdir react-codelab
cd react-codelab
git init
git remote add -f origin https://github.com/velopert/react-codelab-project.git
git checkout step00
```
```
git checkout step10
git checkout -- *.*
```

## MongoDB Start `32bit`
```
mongod --dbpath d:\DevelopN\data\db --journal
```


## npm
```
npm install -g webpack webpack-dev-server
npm install -g babel-cli nodemon cross-env
npm install --save express body-parser
npm install --save react-hot-loader@1.3.0
npm install --save morgan body-parser
npm install --save mongoose express-session
npm install --save bcryptjs
npm install --save axios react-addons-update react-router react-timeago redux react-redux redux-thunk
npm install --save-dev style-loader css-loader
```